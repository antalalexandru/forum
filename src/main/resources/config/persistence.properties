persistence.member.get_all_members=SELECT * FROM members INNER JOIN groups ON members.group_id = groups.group_id
persistence.member.get_member=SELECT * FROM members INNER JOIN groups ON members.group_id = groups.group_id WHERE members.member_id = ?